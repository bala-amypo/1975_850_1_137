# 🚩 College Portal Requirement
spring.application.name=demo

# 🌐 Portal uses port 9001
server.port=9001
server.use-forward-headers=true

# ============================
# 🛢️ DATABASE CONFIG
# ============================

# 👉 Use MySQL if available (Portal)
spring.datasource.url=jdbc:mysql://localhost:3306/saasdb?createDatabaseIfNotExist=true&allowPublicKeyRetrieval=true&useSSL=false
spring.datasource.username=root
spring.datasource.password=Amypo
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# 👉 Fallback to H2 (Local Run) IF MySQL not running
spring.datasource.hikari.initialization-fail-timeout=0

# ============================
# 🔧 JPA / Hibernate
# ============================
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# ============================
# 🌐 CORS (Portal required)
# ============================
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=*
spring.web.cors.allowed-headers=*
